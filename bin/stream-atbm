#!/bin/bash

update-mixes
echo "running server!"
ezstream -c ../configs/atbm-radio.xml

# find /songs/ -name '*.mp3' > /tmp/sorted.txt && \
# ezstream -s /tmp/sorted.txt > /tmp/station1.txt && \
echo "exiting server!"
